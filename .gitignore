# Terraform
.terraform/
*.tfstate
*.tfstate.*
crash.log
*.tfvars
!example.tfvars
# .terraform.lock.hcl IS committed (pins provider versions/hashes for reproducible init in CI).

# Env / secrets
.env
.env.*

# OS / editor
.DS_Store
.idea/
.vscode/
